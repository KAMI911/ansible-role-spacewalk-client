---
# handlers file for ansible-role-spacewalk-client

- name: restart spacewalk client
  service:
    name: '{{ spacewalk_service_name }}'
    state: '{{ spacewalk_service_status }}'
    enabled: '{{ spacewalk_service_enabled }}'
  listen: 'restart spacewalk service'
